fastapi==0.141.1         # Utilisé dans app.py, auth.py (FastAPI, Request, Reponses, etc.)
sqlmodel==0.0.39         # Utilisé dans app.py, models.py (Session, SQLModel, Field, select)
SQLAlchemy==2.0.52       # Utilisé dans database.py, models.py (Column, ForeignKey, Integer, String)
uvicorn==0.52.4          # Utilisé dans app.py (serveur ASGI pour lancer l'application)
pydantic==2.13.4         # Utilisé dans app.py (BaseModel pour la validation des données)
starlette==1.6.0         # Utilisé dans app.py (SessionMiddleware pour la gestion des sessions)
python-dotenv==1.2.3     # Utilisé dans app.py, auth.py (load_dotenv pour charger les variables d'environnement)
requests==2.34.2         # Utilisé dans auth.py (appels à l'API Microsoft Graph)
msal==1.38.0             # Utilisé dans auth.py (Authentification Microsoft Entra ID)
openpyxl==3.1.5          # Utilisé dans app.py (load_workbook pour l'import d'étudiants via Excel)
python-multipart==0.0.32 # Requis par FastAPI dans app.py (pour Form et UploadFile)
Jinja2==3.1.6            # Requis par FastAPI dans app.py (pour Jinja2Templates)
itsdangerous==2.2.0      # Requis par Starlette dans app.py (pour SessionMiddleware)
pandas==3.0.5            # Utilisé pour l'agrégation des données et l'export CSV
requests-cache==1.3.3    # Utilisé pour mettre en cache les requêtes LLM
markdown-it-py==4.2.0    # Utilisé pour transformer le markdown du LLM en HTML
